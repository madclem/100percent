{
  "routes": [
    {
      "method": "GET",
      "path": "/milestones",
      "handler": "Milestones.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/milestones/count",
      "handler": "Milestones.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/milestones/:_id",
      "handler": "Milestones.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/milestones",
      "handler": "Milestones.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/milestones/:_id",
      "handler": "Milestones.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/milestones/:_id",
      "handler": "Milestones.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}